{
  "$schema": "https://railway.app/railway.schema.json",
  "deploy": {
    "startCommand": "php -r \"var_dump($_ENV['APP_KEY'] ?? 'NOT_SET');\" && php artisan serve --host=0.0.0.0 --port=$PORT"
  }
}